<%= form_for @project do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <div class="field">
    <%= f.text_area :project_name %>
  </div>
  <div class="actions">
    <%= f.submit "Submit" %>
  </div>
<% end %>
